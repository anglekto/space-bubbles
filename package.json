{
  "private": true,
  "name": "space-bubbles",
  "scripts": {
    "start": "browser-sync start --server public --files public",
    "deploy": "netlify deploy --prod --dir=public",
    "setup-deploy": "app-app --deploy && yarn deploy",
    "postdeploy": "echo 'Your site was deployed to https://space-bubbles-angleto.netlify.com'"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "stylelint": "^11.0.0",
    "stylelint-config-standard": "^19.0.0"
  },
  "homepage": "https://space-bubbles-angleto.netlify.com"
}